import { FunctionComponent } from "react";

interface CheckingWeekDataProps {
    setStep: (arg0: string) => void
}

const CheckingWeekData: FunctionComponent<CheckingWeekDataProps> = ({ setStep }) => {
    return (
        <div>
            CheckingWeekData
        </div>
    )
}

export default CheckingWeekData;